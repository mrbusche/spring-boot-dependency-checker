#!/usr/bin/env node

import { checkDependencies } from '../index.js';

process.title = 'spring-boot-dependency-checker';

const output = await checkDependencies(process.argv[2]);
if (output) {
    console.log(JSON.stringify(output, null, 2));
}
